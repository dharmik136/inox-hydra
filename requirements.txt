# Core Backend & Gateway
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0

# Image & PDF Processing (1080x1080 Carousel Engine)
pillow>=10.2.0
pymupdf>=1.24.0

# Background Queue Dispatcher (studio/backend/scheduler.py)
apscheduler>=3.10.0

# Multipart form parsing (media dropzone uploads)
python-multipart>=0.0.9

# Networking & API Integrations
requests>=2.31.0
httpx>=0.27.0

# Testing & Quality Assurance
pytest>=8.0.0
flake8>=7.0.0

# Only the test suite imports yaml, to parse the release workflow and check it
# still runs the build and still pins windows-latest. It was absent here, so
# that test importorskip'd on every CI run while passing on developer machines
# that happened to have pyyaml. The check on the release pipeline was the one
# thing not being checked.
pyyaml>=6.0
