{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The splash window only. The studio itself is served over loopback and uses no Tauri APIs, so nothing here is granted to the remote origin.",
  "windows": ["studio"],
  "permissions": [
    "core:default"
  ]
}
